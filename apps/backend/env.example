# Environment
ENV=dev

# Database
# - Docker Postgres (docker-compose): 아래 값 사용 (호스트에서 접속 시)
# - Railway 등 원격: 해당 URL로 교체
DATABASE_URL=postgresql://ddangha_user:ddangha_password@localhost:5432/ddangha_db

# API Settings
API_PREFIX=/api/v1
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173
